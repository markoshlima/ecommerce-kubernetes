apiVersion: v1
kind: Service
metadata:
  name: compra-k8s-service
  namespace: compra-k8s
  labels:
    app: compra-k8s-app
spec:
  selector:
    app: compra-k8s-app
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 8080